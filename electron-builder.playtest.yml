extends: electron-builder.yml

productName: A Dark Cave Playtest

win:
  target:
    - nsis
  icon: build-resources/icon.png
  executableName: A Dark Cave
  artifactName: ${productName}-${version}-setup.${ext}
