
import { GameEvent } from "./events";

export const caveExploreEvents: Record<string, GameEvent> = {
  // Cave exploration specific events can be added here
  // For now, this is a placeholder for future cave-specific events
};
