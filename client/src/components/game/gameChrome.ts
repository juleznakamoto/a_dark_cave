/** Pause overlay insets — keep in sync with header/footer padding + control height. */
export const GAME_HEADER_INSET = "36px";
export const GAME_FOOTER_INSET = "36px";

/** Top inset for the side-panel column and game tab nav — keep in sync. */
export const GAME_PANEL_HEADER_ROW_CLASS = "pt-2 md:pt-3";

/** Bottom-aligned label row shared by game tabs and side-panel section titles. */
export const GAME_PANEL_HEADER_LABEL_ROW =
  "inline-flex items-end pb-2 text-sm leading-none";
