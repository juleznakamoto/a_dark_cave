node_modules
dist
dist-electron
release
steam/content
steam/output
steam/generated
steam/config.local.json
.DS_Store
.env*
!.env.example
!**/.env.example
server/public
vite.config.ts.*
*.tar.gz
replit.md

# Video analysis / private transcripts
_analysis/

# Replit internal dev artifacts (pasted console logs & debug screenshots)
attached_assets/

# Gender service (internal Python microservice) — match wherever the folder lives
**/gender-service/venv
**/gender-service/.env
**/gender-service/first_names_base.db

# Resend contact exports (PII — never commit)
exports/

# Resend broadcast HTML, hero assets, and local email build scripts (not in repo)
marketing/
client/public/email/
scripts/build-marketing-email-social-row.mjs

# Cursor IDE — local only (MCP keys, rules, hooks, settings)
.cursor/
